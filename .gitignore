node_modules/
.cache/
*.log
.DS_Store

# Outputs of sources whose text is not redistributable (license.redistributable: false).
# Build them locally: `bun run build <id>`. validate enforces these entries.
ir/apple-hig/
skills/apple-hig/
ir/wcag22/
skills/wcag22/
evals/agent-results.json

# Cached source pages backing measurement records. Apple's text, so local-only like ir/apple-hig/.
# The records commit each page's sha256 instead, which identifies the content without republishing it.
guidance/*/records/snapshots/
