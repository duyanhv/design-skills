{
  "name": "design-skills",
  "version": "0.1.0",
  "description": "Compile platform design guidelines (Apple HIG, Material 3, WCAG, …) into versioned, cited, LLM-usable Agent Skills.",
  "type": "module",
  "private": true,
  "license": "MIT",
  "scripts": {
    "ds": "bun run src/cli.ts",
    "fetch": "bun run src/cli.ts fetch",
    "normalize": "bun run src/cli.ts normalize",
    "extract": "bun run src/cli.ts extract",
    "compose": "bun run src/cli.ts compose",
    "validate": "bun run src/cli.ts validate",
    "build": "bun run src/cli.ts build",
    "typecheck": "tsc --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.70.0",
    "yaml": "^2.8.0",
    "zod": "^4.1.0"
  },
  "devDependencies": {
    "@types/bun": "^1.2.0",
    "typescript": "^5.9.0"
  }
}
