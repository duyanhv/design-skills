# Deterministic evals for the generated wcag22 skill: `bun run eval wcag22`.
#
# `expect` is a page-level smoke check. `rule` locates one success criterion and asserts what an
# auditor acts on: its conformance level, the exceptions that make it satisfiable, and the anchor a
# finding should cite. The exception assertions are the regression guard for AUDIT finding 1, where
# a criterion kept its numbers but lost the list that gave them meaning.

- q: What is the minimum contrast ratio for normal text at Level AA, and what is exempt?
  source: distinguishable
  expect: ["4.5:1", "Level AA"]
  rule:
    statement_contains: contrast ratio of at least 4.5:1
    conformance_level: AA
    anchor: contrast-minimum
    # Quoting 4.5:1 without the exemptions produces false findings on logos and disabled controls.
    context_contains:
      - "Large Text"
      - "Incidental"
      - "Logotypes"
    rendered_contains: ["**(Level AA)**"]

- q: What are the four text-spacing values a page must survive?
  source: distinguishable
  # Regression for AUDIT finding 1: all four requirements were rendered as empty bullets.
  rule:
    statement_contains: no loss of content or functionality occurs by setting all of the following
    conformance_level: AA
    anchor: text-spacing
    context_contains:
      - "Line height (line spacing) to at least 1.5 times the font size"
      - "Spacing following paragraphs to at least 2 times the font size"
      - "Letter spacing (tracking) to at least 0.12 times the font size"
      - "Word spacing to at least 0.16 times the font size"

- q: What must be true of content shown on hover or focus?
  source: distinguishable
  # Regression for AUDIT finding 1: Dismissible/Hoverable/Persistent lost most of their definitions.
  rule:
    statement_contains: removing pointer hover or keyboard focus triggers additional content
    conformance_level: AA
    anchor: content-on-hover-or-focus
    context_contains:
      - "Dismissible — A mechanism is available to dismiss the additional content without moving pointer hover or keyboard focus"
      - "Hoverable — If pointer hover can trigger the additional content"
      - "Persistent — The additional content remains visible until the hover or focus trigger is removed"

- q: What viewport widths and heights must content reflow to without two-dimensional scrolling?
  source: distinguishable
  rule:
    statement_contains: without loss of information or functionality, and without requiring scrolling in two dimensions
    conformance_level: AA
    anchor: reflow
    context_contains:
      - "Vertical scrolling content at a width equivalent to 320 CSS pixels"
      - "Horizontal scrolling content at a height equivalent to 256 CSS pixels"

- q: What is the minimum target size at Level AA (2.5.8), and when does it not apply?
  source: input-modalities
  expect: ["24 by 24 CSS pixels"]
  rule:
    statement_contains: at least 24 by 24 CSS pixels
    conformance_level: AA
    anchor: target-size-minimum
    context_contains: ["Spacing", "Equivalent", "Inline", "Essential"]

- q: Is 4.1.1 Parsing still a requirement in WCAG 2.2?
  source: compatible
  expect: ["Parsing (Obsolete and removed)"]

- q: How long may audio auto-play before a pause/stop control is required?
  source: distinguishable
  expect: ["3 seconds"]
  rule:
    statement_contains: plays automatically for more than 3 seconds
    conformance_level: A
    anchor: audio-control

- q: Which level is Focus Not Obscured (Minimum), and is it new in 2.2?
  source: navigable
  expect: ["Focus Not Obscured (Minimum)", "Level AA"]
  rule:
    statement_contains: the component is not entirely hidden due to author-created content
    conformance_level: AA
    anchor: focus-not-obscured-minimum

- q: Is an AAA criterion presented as optional, or as normative text selected by the target level?
  source: distinguishable
  # Regression for AUDIT finding 4: AAA used to be rendered MAY, which misstates WCAG's model.
  rule:
    statement_contains: contrast ratio of at least 7:1
    conformance_level: AAA
    severity: must
    anchor: contrast-enhanced

- q: Where are terms like "large scale (text)" defined?
  source: glossary
  expect: ["large scale (text)", "18 point"]

# --- A2: normative conditions vs. informative notes ---

- q: Does 2.5.3 Label in Name require the visible label at the *start* of the accessible name?
  source: input-modalities
  # It does not: the criterion requires containment, and the ordering advice is a Note, which W3C
  # classifies as informative. Rendering it unlabelled beside the exceptions made an agent fail a
  # conforming component on ordering alone (audit A2). The label is what stops that.
  rule:
    statement_contains: the name contains the text that is presented visually
    conformance_level: A
    anchor: label-in-name
    context_contains:
      - "_[informative]_ Note: A best practice is to have the text of the label at the start of the name."
    rendered_contains:
      - "_[informative]_ Note: A best practice is to have the text of the label at the start of the name."

- q: Are 2.5.8's exceptions advisory, or conditions of the criterion?
  source: input-modalities
  # Conditions. An exception must *not* pick up the informative label, or the split would be
  # decoration: it would tell a reader to disregard the Spacing exception that makes a 20px target
  # conforming. The two assertions below are the same guard from both sides.
  rule:
    statement_contains: at least 24 by 24 CSS pixels
    conformance_level: AA
    anchor: target-size-minimum
    context_contains:
      - "Spacing — Undersized targets"
      - "Essential — A particular presentation of the target is essential"
      - "_[informative]_ Note: Targets that allow for values to be selected spatially"
    rendered_contains:
      - "- Spacing — Undersized targets"
      - "- Essential — A particular presentation of the target is essential"

- q: What does the skill say the `_[informative]_` marker means?
  source: input-modalities
  # The marker is useless if the entry file never explains it. This is a page-level smoke check;
  # the entry-file wording itself is asserted by the wcag-authority probe.
  expect: ["_[informative]_"]

# --- A3: dependencies are reachable, and conformance is its own question ---

- q: Which input purposes does 1.3.5 mean, and can I reach the list from the criterion?
  source: adaptable
  # The criterion's own condition names a section. Extraction used to flatten the link to its
  # label, so the shipped reference named a section with no way to reach it (audit A3).
  rule:
    statement_contains: The purpose of each input field collecting information about the user
    conformance_level: AA
    anchor: identify-input-purpose
    context_contains:
      - "[Input Purposes for user interface components section](#input-purposes)"
    rendered_contains:
      - "](../reference/input-purposes.md#"

- q: Is the defined set of input purposes in the bundle?
  source: input-purposes
  expect:
    - "`cc-exp-month`"
    - "`street-address`"
    - "`bday`"
    - "Input Purposes for User Interface Components"

- q: What does a page have to satisfy to conform, beyond the criteria at its level?
  source: conformance-reqs
  # Selecting A/AA/AAA is cc1 alone. A review that checks criteria and concludes "conforms" has
  # skipped four more requirements; they have to be in the bundle to be checkable at all.
  expect:
    - "Full pages"
    - "Complete processes"
    - "Only Accessibility-Supported Ways of Using Technologies"
    - "Non-Interference"
    - "all web pages in the process conform at the specified level or better"

- q: Which of WCAG's own words create requirements?
  source: conformance-reqs
  # The source's own statement of the rule this skill now follows.
  expect:
    - "diagrams, examples, and notes are informative (non-normative)"
    - "does not create requirements that impact a conformance claim"
